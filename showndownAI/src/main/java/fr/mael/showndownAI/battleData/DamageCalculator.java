package fr.mael.showndownAI.battleData;

public class DamageCalculator {

	
	
	/* 
	 * Voici la formule:

		Dégâts = (((((2 * Niveau / 5 + 2) * AttackStat * puissance D’attaque / étoile de défense) / 50) + 2) * STAB * faiblesse / résistance * nombre aléatoire / 100
		AttackStat = attaque de l’attaquant ou spéciale
		La Puissance d’attaque = puissance d’attaque
		Defencestat = protection du défenseur ou spécial
		STAB = attaque bonus du même type (1 ou 1,5)
		Faiblesse / résistance = modificateurs de type(40, 20, 10, 5, 2.5 ou 0)
		Nombre aléatoire = nombre aléatoire entre 217 et 255
	 */
	
}
